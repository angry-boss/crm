#footer
  Powered by #{link_to "NLMT CRM", "https://www.netzlaboranten.de", :"data-popup" => true }
  v#{CRM::VERSION::STRING} &copy; 2008-#{Date.today.year} by Netzlaboranten G.m.b.H.

  - unless tabless_layout?
    \|
    = link_to(t(:about_ffc), "#", title: t(:about_ffc), onclick: "$('#about').dialog({ title: this.title, modal: true }); return false;")
%br
= render "layouts/about"
